import test from 'node:test';import assert from 'node:assert/strict';import {normalizeGitHubRepository,tokenMatches,validateGitRef} from '../src/security.mjs';
test('normalizes public GitHub URLs',()=>{assert.equal(normalizeGitHubRepository('https://github.com/openai/openai-node'),'https://github.com/openai/openai-node.git');assert.equal(normalizeGitHubRepository('https://github.com/openai/openai-node.git/'),'https://github.com/openai/openai-node.git')});
test('rejects unsafe repository URLs',()=>{assert.throws(()=>normalizeGitHubRepository('http://github.com/owner/repo'),/https:\/\/github\.com/);assert.throws(()=>normalizeGitHubRepository('https://example.com/owner/repo'),/github\.com/);assert.throws(()=>normalizeGitHubRepository('https://token@github.com/owner/repo'),/credentials/);assert.throws(()=>normalizeGitHubRepository('https://github.com/owner/repo/issues'),/form/)});
test('validates refs',()=>{assert.equal(validateGitRef('feature/mobile-loop'),'feature/mobile-loop');assert.throws(()=>validateGitRef('--upload-pack=evil'),/unsafe/);assert.throws(()=>validateGitRef('../main'),/unsafe/);assert.throws(()=>validateGitRef('main@{1}'),/unsafe/)});
test('compares bearer tokens',()=>{assert.equal(tokenMatches('secret-token','Bearer secret-token'),true);assert.equal(tokenMatches('secret-token','Bearer wrong-token'),false);assert.equal(tokenMatches('secret-token',undefined),false)});
